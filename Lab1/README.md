# Lab 1: Environment Setup

### 🎯 Objective
To set up the development environment required for Machine Learning tasks, including Python, Anaconda, and Visual Studio Code.

### ✅ Completed Tasks
1. **Installed Anaconda Navigator:** Successfully set up the base environment.
2. **Configured VS Code:** Linked VS Code with the Anaconda Python interpreter.
3. **Verified Libraries:** Confirmed installation of `pandas`, `numpy`, and `matplotlib`.
4. **Git Configuration:** Initialized the repository and configured user credentials.
